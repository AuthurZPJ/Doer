{
  "name": "doer",
  "private": true,
  "version": "1.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "bin": {
    "doer": "cli/doer.mjs"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "test": "npm run test -w server",
    "electron": "electron .",
    "electron:dev": "concurrently -n server,client,electron -c blue,green,magenta \"npm run dev -w server\" \"npm run dev -w client\" \"wait-on http://localhost:5173 && electron .\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "electron": "^33.0.0",
    "wait-on": "^8.0.0"
  }
}
